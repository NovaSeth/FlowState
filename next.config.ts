import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Hide the Next.js dev indicator (the corner badge) - it is not part of the app.
  devIndicators: false,
};

export default nextConfig;
